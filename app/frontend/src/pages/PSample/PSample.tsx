import { FSample } from "../../features";

export const PSample: React.FC = (): React.JSX.Element => {
    return (
        <>
            <h1><center>This is a sample page</center></h1>
            <FSample/>
        </>
    );
}